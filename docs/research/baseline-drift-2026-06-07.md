# تقرير انحراف بصري — baseline-drift-2026-06-07

**تاريخ التشغيل:** 2026-06-07 (جولة أسبوعية — الأحد)
**مرجع الـ baseline:** `docs/design/baseline/marketplace-home.png` و `docs/design/baseline/subdomain-store.png`
**التقرير السابق:** `docs/research/baseline-drift-2026-06-05.md` (6 انحرافات مفتوحة)

---

## ملخص تنفيذي

**جميع الانحرافات الستة من تقرير 2026-06-05 لا تزال قائمة ولم يُعالَج منها شيء.**

أضاف commit اليوم (`945a609 — fix(storefront): UX batch`) انحرافًا **سابعًا جديدًا** في ارتفاع hero المتجر الفرعي.

وفق قواعد الحارس: لا تُكمل المرحلة 2 (استخبارات منافسين) قبل مراجعة هذا التقرير، ولا تُنتج مرحلة 3 (spec) حتى يُعالج drift وجود.

---

## حالة الانحرافات السابقة (2026-06-05)

| # | المكوّن | الملف | الأولوية | الحالة اليوم |
|---|---------|-------|----------|--------------|
| 1 | Hero features row | `app/page.tsx:169` | 🔴 عالية | **لم يُعالج** |
| 2 | Hero search dropdown | `app/page.tsx:169` | 🟡 متوسطة | **لم يُعالج** |
| 3 | StatsBar | `app/page.tsx` | 🔴 عالية | **لم يُعالج** |
| 4 | ProductTile heart | `app/page.tsx:91-98` | 🟡 متوسطة | **لم يُعالج** |
| 5 | ProductTile cart shape | `app/page.tsx:106` | 🟢 منخفضة | **لم يُعالج** |
| 6 | ProductCard heart | `components/product/ProductCard.tsx:25-40` | 🟡 متوسطة | **لم يُعالج** |

**مرجع التفاصيل والتوصيات:** `docs/research/baseline-drift-2026-06-05.md`

---

## الانحراف 7 — StoreHeader: تصغير hero المتجر الفرعي (جديد اليوم)

**الأولوية:** 🟡 متوسطة
**الملف:** `components/store/StoreHeader.tsx` — السطر 92
**المكوّن:** Hero (store subdomain)
**commit المُسبِّب:** `945a609` — `fix(storefront): UX batch` (2026-06-07 05:54 AM)

**الـ baseline يُظهر:**
بانر عريض بلغة بصرية سينمائية (خلفية داكنة، خطوط متوهجة، عناصر ثلاثية الأبعاد)، موثّق في `components-inventory.md` كـ"بانر عريض مخصص لفيديو دعائي متحرك" — يُفترض ارتفاع يليق بمحتوى فيديو.

**الوضع الراهن قبل commit 945a609:**
```
h-72 overflow-hidden … md:h-[360px]
```
ارتفاع: 288px موبايل / 360px ديسكتوب

**الوضع الراهن بعد commit 945a609:**
```tsx
className={`store-hero-motion store-hero-${motion} relative h-36 overflow-hidden bg-[var(--muted)] md:h-52`}
```
ارتفاع: 144px موبايل / 208px ديسكتوب — تقليص بنسبة 50% و42% تباعاً.

**التأثير الثانوي — StoreInfoCard float:**
تغيّر offset البطاقة العائمة من `-mt-20 md:-mt-24` إلى `-mt-12 md:-mt-16` لتعويض الارتفاع الجديد. هذا يُقلص البروز البصري للبطاقة العائمة على الـ hero.

**متى تغيّر:**
commit `945a609` — اليوم 2026-06-07، وصف الـ commit: `StoreHeader: shrink hero (h-72→h-36/md h-52)`.

**توصية الاسترجاع:**
استرجاع الارتفاع الأصلي في السطر 92 من `StoreHeader.tsx`:
```
h-36 md:h-52  →  h-72 md:h-[360px]
```
مع استرجاع offset البطاقة العائمة في السطر 143:
```
-mt-12 md:-mt-16  →  -mt-20 md:-mt-24
```
ملاحظة: إن كان الهدف تصغير فعلي مقبول من الفريق، يجب تحديث baseline رسمياً عبر PR منفصل إلى `docs/design/baseline/`.

---

## المرحلة 2 — استخبارات المنافسين: مؤجلة

**السبب:** جميع محاولات WebFetch على مصادر المنافسين (salla.sa، salla.com/themes، docs.salla.dev، zid.sa، help.zid.sa، shopify.com/examples) أعادت `HTTP 403 Forbidden` في هذه الجولة.

**حالة exa:** الأداة متاحة في البيئة (`mcp__exa`) لكنها تتطلب تفويض OAuth من المالك — لم يكتمل التفويض في هذه الجلسة.

**تقرير المرجع القائم:** `docs/research/competitors/2026-23.md` (2026-06-01 → 2026-06-07) — لا delta جديد مُوثَّق هذه الجولة.

**الإجراء المطلوب:** تفعيل exa OAuth في إعدادات الـ Route لتمكين الفحص الصحيح في الجولة القادمة.

---

## المرحلة 3 — Spec: محظورة

وجود drift نشط يمنع إنتاج spec جديد في هذه الجولة وفق قواعد الحارس.

---

## الخطوة التالية المطلوبة

1. **معالجة الانحرافات 1-7** عبر Cursor قبل الجولة القادمة — أو تحديث baseline رسمياً إن كان التصغير مقصوداً (drift #7).
2. **تفعيل exa OAuth** في إعدادات الـ Route لتمكين فحص المنافسين بشكل صحيح.
3. بعد اكتمال المعالجة → الجولة التالية تنتج spec للانحراف ذي الأولوية الأعلى.
