# تقرير استخبارات المنافسين — 2026-W32

**تاريخ التشغيل:** 2026-07-08
**الأساس الزمني:** `docs/research/competitors/2026-29.md` (2026-06-16)
**حالة exa:** متاحة ✓ — أول تشغيل بـ exa بعد 7 جولات بدونها

---

## ملاحظة منهجية

استُخدمت exa للبحث في:
- `docs.salla.dev` changelog و Twilight releases (يونيو–يوليو 2026)
- `github.com/Shopify/dawn` releases (أحدث إصدار)
- `zid.engineering` و `docs.zid.sa` (تحديثات Vitrin)

---

## Delta جديد — W32

### 1. Salla Twilight v2.14.425 — إدارة تفضيلات ملفات تعريف الارتباط

| التفصيل | القيمة |
|---------|--------|
| المصدر | `docs.salla.dev/888746f0` — changelog مؤرخ 2026-06-24 |
| الوصف | إضافة cookie preference management داخل مكوّن `salla-menu` — يتيح للزائر التحكم في موافقته على ملفات تعريف الارتباط من القائمة الرئيسية مباشرةً |
| النمط البصري | زر أو رابط "إدارة الخصوصية" داخل قائمة التنقل، يفتح نافذة تفضيلات مصغّرة |
| الانطباق على dasm-stores | **لا** — dasm-stores لا تعرض أي cookie consent UI حتى الآن. هذه ميزة امتثال تنظيمي تختلف عن نطاق baseline المتسوق |
| الملف المتأثر | لا ينطبق على الـ baseline الحالي |
| Delta جديد؟ | **نعم** — لم يُوثَّق سابقاً |

### 2. Shopify Dawn 15.5.0 — مقاطع الإفصاح على صفحة المنتج

| التفصيل | القيمة |
|---------|--------|
| المصدر | `github.com/Shopify/dawn` releases — v15.5.0 (أحدث إصدار) |
| الوصف | إضافة section وblock جديدين من نوع "Product Disclosures" يُعرضان على صفحة تفصيل المنتج — نصوص إفصاح قابلة للتهيئة (ضمانات، سياسة إرجاع، شهادات، تحذيرات منتج). تظهر عادةً أسفل زر "أضف للسلة" |
| النمط البصري | قسم قابل للطيّ (accordion) أو بطاقات مقسّمة، كل بطاقة تحمل عنوان الإفصاح ونصه. لون خلفية محايد يميّزها عن وصف المنتج |
| الانطباق على dasm-stores | **جزئياً** — `app/[slug]/products/[productId]/page.tsx` لا يعرض أي قسم إفصاح. البيانات (ضمان، إرجاع) قد تكون متاحة من `getStore()` response في حقل `settings` |
| الملف المتأثر | `app/[slug]/products/[productId]/page.tsx` |
| Delta جديد؟ | **نعم** — الإصدار 15.5.0 جديد. الإفصاحات لم تُذكر في تقارير سابقة |

### 3. Shopify Dawn 15.5.0 — تفاعلات السلة للـ AI والعملاء الذكيين

| التفصيل | القيمة |
|---------|--------|
| المصدر | `github.com/Shopify/dawn` releases — v15.5.0 |
| الوصف | دعم "Storefront Events & Actions" يتيح لتطبيقات AI وعملاء ذكيين تحديث السلة دون إعادة تحميل الصفحة |
| الانطباق على dasm-stores | **لا** — ميزة بنية تحتية للـ platform، خارج نطاق baseline المتسوق |
| Delta جديد؟ | **نعم** — مذكور للأرشفة فقط، لا spec |

### 4. Zid Vitrin — هيكل جديد لإعدادات المتجر

| التفصيل | القيمة |
|---------|--------|
| المصدر | `docs.zid.sa/store-settings-mapping` |
| الوصف | منصة Vitrin الجديدة أعادت هيكلة إعدادات المتجر تحت `store.settings.*` — wishlist_enabled، low_stock_label_enabled، payment widgets كلها انتقلت لمسارات موحّدة |
| الانطباق على dasm-stores | **لا** — تغيير في API المتجر الداخلي لـ Zid، لا يُترجم لـ pattern بصري |
| Delta جديد؟ | **نعم** — مذكور للأرشفة، يُشير إلى أن Zid يوحّد نموذج البيانات نحو feature flags متجر (wishlist، stock labels) |

---

## أنماط مستمرة من W29 (لا delta جديد)

| النمط | المنافس | الانطباق | الملف | الحالة |
|-------|---------|----------|-------|--------|
| Wishlist hooks على مستوى platform | Salla v2.14.400 | نعم | `components/product/ProductCard.tsx` | specs معلقة |
| salla-fulfillment-methods على PDP | Salla | جزئياً | `app/[slug]/products/[productId]/page.tsx` | ideas-backlog |
| Quick Buy من صفحة الكتالوج | Salla, Zid | نعم | `components/product/ProductCard.tsx` | spec معلق |
| Sticky add-to-cart (mobile) | Shopify, Salla | نعم | صفحة المنتج | spec معلق |

---

## الخطوة التالية

- **لا spec هذه الجولة** — drift مانع (انظر `baseline-drift-2026-07-08.md`)
- **الجولة القادمة (W33):** إن حُدِّث baseline المتجر البصري، أولوية الـ spec:
  - إفصاحات المنتج (product-disclosures) — مُلهَم من Dawn 15.5.0 — أثر عالٍ / جهد متوسط
  - seller CTA في الهيدر — مُلهَم من Shopify header — أثر عالٍ / جهد منخفض (spec `home-header-seller-cta-2026-06-16.md` موجود، ينتظر تنفيذ Cursor)
- exa أثبتت فاعليتها هذه الجولة — تُوثَّق كمصدر أساسي مُعتمد من W32 فصاعداً
