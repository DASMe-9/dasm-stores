# تقرير استخبارات المنافسين — 2026-W31

**تاريخ التشغيل:** 2026-08-03
**الأساس الزمني:** `docs/research/competitors/2026-29.md` (2026-06-16) — الأسابيع W30-W31
**حالة exa:** متاحة هذه الجولة (أول استخدام فعلي)
**نطاق الفحص:** Salla changelog، Zid blog، Shopify Dawn releases

---

## Delta جديد — W31

### 1. Salla Twilight v2.14.440 — Guest Wishlist في بطاقة المنتج

| التفصيل | القيمة |
|---------|--------|
| المصدر | `docs.salla.dev` — Twilight changelog v2.14.440 |
| الوصف | إضافة **دعم Wishlist للزوار غير المسجّلين** في `salla-product-card` و`salla-products-list` عبر `WishlistApi`. الزوار يحفظون المفضلة client-side دون تسجيل دخول |
| النمط | قلب على بطاقة المنتج يعمل فوراً للجميع؛ يُزامَن مع الحساب عند تسجيل الدخول |
| الانطباق على dasm-stores | **نعم** — يؤكد ويوسّع نطاق `product-card-store-wishlist-2026-06-12.md` و`product-tile-wishlist-2026-06-11.md`: التنفيذ المقترح يكون localStorage أولاً (بلا auth)، مع خيار مزامنة لاحق |
| الملف المتأثر | `components/product/ProductCard.tsx`، `app/page.tsx` (ProductTile) |
| Delta جديد؟ | **نعم** — Guest wishlist (localStorage) لم يُوثَّق سابقاً؛ W29 وثّق الـ wishlist hooks فقط لمستخدمين مسجّلين |

### 2. Salla Twilight v2.14.490 — salla-cart-summary-card

| التفصيل | القيمة |
|---------|--------|
| المصدر | `docs.salla.dev` — Twilight changelog v2.14.490 |
| الوصف | مكوّن `salla-cart-summary-card` جديد لعرض ملخص السلة. يظهر بعد إضافة منتج كتأكيد خفيف بدلاً من فتح drawer كامل: صورة مصغّرة للمنتج، الاسم، السعر، عداد، زر "إتمام الشراء" |
| النمط | popup/toast تظهر أسفل أو جانب الزر الذي أضاف المنتج، تُغلق تلقائياً بعد ثوانٍ أو بالنقر خارجها |
| الانطباق على dasm-stores | **نعم** — `CartDrawer.tsx` يفتح درار كاملاً. لا يوجد تأكيد خفيف بعد إضافة منتج. يُطابق ideas-backlog 2026-06-15 "Cart Confirmation Popup" |
| الملف المتأثر | مكوّن جديد `components/cart/CartToast.tsx` + `store/cartStore.ts` + `components/store/StoreChrome.tsx` |
| Delta جديد؟ | **نعم** — v2.14.490 لم يُوثَّق في W29 |

### 3. Shopify Dawn 15.5.0 — Product Disclosures Section

| التفصيل | القيمة |
|---------|--------|
| المصدر | GitHub `Shopify/dawn` release v15.5.0 (19 يونيو 2026) |
| الوصف | قسم وبلوك جديدان لعرض "product disclosures" على صفحة المنتج: معلومات تشريعية، مكوّنات، شهادات. مستقلان عن وصف المنتج الرئيسي |
| الانطباق على dasm-stores | **جزئياً** — `app/[slug]/products/[productId]/page.tsx` لا يعرض أي معلومات تشريعية/مكوّنات. يتوقف على توافر الحقول في API الخاص بالمنتج |
| الملف المتأثر | `app/[slug]/products/[productId]/page.tsx` |
| Delta جديد؟ | **نعم** — v15.5 (بعد W29) |

### 4. Salla Twilight v2.14.490 — AI Cart Interactions

| التفصيل | القيمة |
|---------|--------|
| المصدر | `docs.salla.dev` — Twilight changelog v2.14.490 |
| الوصف | دعم تفاعلات السلة من الـ AI agents (إضافة منتجات، تعديل الكميات) بدون إعادة تحميل الصفحة عبر Storefront Events |
| الانطباق على dasm-stores | **لا** — منظومة AI cart خارج نطاق الجولة الحالية |
| Delta جديد؟ | نعم — لكن منخفض الأولوية |

---

## أنماط مستمرة من W29 (لا delta جديد)

| النمط | المنافس | الانطباق | الحالة |
|-------|---------|----------|--------|
| Wishlist hooks (مستخدمون مسجّلون) | Salla v2.14.400 | نعم | موثّق W29 — spec موجود |
| fulfillment-methods على صفحة المنتج | Salla | جزئياً | ideas-backlog (تحقق API مطلوب) |
| Sticky Add-to-Cart (mobile) | Shopify, Salla | نعم | spec موجود `sticky-mini-cart-bar-2026-06-15.md` |
| Seller CTA في الهيدر | Shopify | نعم | ✅ **نُفّذت** (W29 spec، commit `013f987`) |

---

## ملاحظات على Zid — W31

لم تُرصَد تغييرات واجهة متسوق من Zid منذ W29. التحديثات المتاحة تخص:
- شريط علوي جديد (إعلانات) — ميزة تاجر
- بحث المخزون — ميزة لوحة تحكم
- تطوير الكاشير — offline POS

غياب تحديثات shopper UI من Zid في هذه الجولة.

---

## الخطوة التالية

**spec هذه الجولة (W31):** CartToast — تأكيد إضافة منتج للسلة (مُلهَم من `salla-cart-summary-card`).
- الأثر العالي: يُحسّن تجربة كل عملية إضافة للسلة
- الجهد المنخفض: مكوّن standalone + state بسيط
- التحقق المستقل: Salla (v2.14.490) + ideas-backlog (2026-06-15) + Shopify (كان سابقاً)
