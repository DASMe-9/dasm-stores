# تقرير استخبارات المنافسين — 2026-W30

**تاريخ التشغيل:** 2026-06-17
**الأساس الزمني:** `docs/research/competitors/2026-29.md` (2026-06-16)
**حالة exa:** لم تُفعَّل — الجولة الثامنة على التوالي. الإجراء المطلوب: Connectors → exa → Authorize (OAuth).

---

## ملاحظة منهجية

exa غير متاحة هذه الجولة. استُخدمت:
- WebSearch على `changelog.salla.sa` و `docs.salla.dev`
- WebSearch على `help.zid.sa` و Zid Developers Portal
- WebSearch على Shopify themes community و Dawn GitHub 2026

---

## Delta جديد — W30

### 1. Salla Twilight v2.14.420 — تنقل في الـ Carousel عبر خيارات اللون

| التفصيل | القيمة |
|---------|--------|
| المصدر | `changelog.salla.sa` — Twilight v2.14.420 |
| الوصف | في مكوّن `salla-product-options`، أصبح اختيار لون من قائمة الخيارات يُشغّل تلقائياً التنقل في صور الـ carousel لعرض صور تلك المتغيرة. الخيار يرتبط بالصورة المقابلة للـ variant. |
| النمط البصري | نقاط ألوان صغيرة دائرية أسفل اسم المنتج؛ النقر على لون يُظهر صورة تلك المتغيرة دون مغادرة الصفحة |
| الانطباق على dasm-stores | **نعم** — `components/product/ProductCard.tsx` لا يعرض أي خيار من variants. `StoreProductCard.variants` يحمل `option_values: Record<string, string>` — البيانات متاحة في الـ response |
| الملف المتأثر | `components/product/ProductCard.tsx` |
| Delta جديد؟ | **نعم** — لم يُوثَّق سابقاً؛ يختلف عن مكوّن wishlist أو cart |

### 2. Salla — `format=light` على List Products Endpoint

| التفصيل | القيمة |
|---------|--------|
| المصدر | `docs.salla.dev` — Merchant API changelog |
| الوصف | أضافت Salla معامل `format=light` لنقطة نهاية قائمة المنتجات؛ يُعيد حقولاً مختصرة (id, name, price, image) دون variants أو descriptions — مناسب لصفحات العرض السريع |
| الانطباق على dasm-stores | **جزئياً** — `lib/api-server.ts` يستدعي list products بدون format. إضافة `format=light` ممكنة للمتاجر الكبيرة، لكنها تلغي بيانات variants المطلوبة للـ spec أعلاه |
| الملف المتأثر | `lib/api-server.ts` — `getProducts()` |
| Delta جديد؟ | **نعم** — يُضاف لـ ideas-backlog كتحسين أداء مستقبلي |

### 3. Shopify Horizon — Color Swatches مدمجة على Product Card

| التفصيل | القيمة |
|---------|--------|
| المصدر | `litextension.com` — Shopify themes 2026 review + `prateeksha.com` best Shopify themes 2026 |
| الوصف | قالب Horizon (الجديد من Shopify) يجعل نقاط ألوان الـ variants **معياراً مرئياً** على بطاقة المنتج في grid. النقر على نقطة لون يُغير الصورة الرئيسية للبطاقة دون انتقال إلى صفحة المنتج |
| النمط | ٣–٥ دوائر ملونة أسفل السعر؛ دائرة نشطة بحد بارز؛ overflow: "+N ألوان" إن تجاوزت ٥ |
| الانطباق على dasm-stores | **نعم** — يُعزز delta رقم 1 (Salla). كلا المنافسين يتجهان للنمط ذاته. `ProductCard.tsx` يفتقر إليه كلياً |
| الملف المتأثر | `components/product/ProductCard.tsx` |
| Delta جديد؟ | **نعم** — تأكيد إضافي على نفس النمط من منافس مختلف |

### 4. Zid — ترحيل قوالب Twig إلى Vitrin

| التفصيل | القيمة |
|---------|--------|
| المصدر | `docs.zid.sa` |
| الوصف | قوالب Twig القديمة أصبحت deprecated؛ الانتقال إلى محرك Vitrin (React-based) يوحّد تجربة المطور |
| الانطباق على dasm-stores | **لا** — تغيير معماري داخلي في Zid. dasm-stores يعتمد Next.js بالفعل |
| Delta جديد؟ | لا — غير قابل للتطبيق |

---

## أنماط مستمرة من W29 (لا delta جديد)

| النمط | المنافس | الانطباق | الملف | الحالة |
|-------|---------|----------|-------|--------|
| Quick Buy من صفحة الكتالوج | Salla | نعم | `components/product/ProductCard.tsx` | spec جاهز `product-card-quick-add-2026-06-13.md` |
| زر سلة دائري على ProductTile | Salla, Zid | نعم | `app/page.tsx` | spec جاهز `product-tile-cart-button-2026-06-14.md` |
| Sold-out overlay على بطاقة المنتج | Shopify, Salla | نعم | `components/product/ProductCard.tsx` | spec جاهز `product-card-sold-out-overlay-2026-06-14.md` |
| CTA استقطاب بائع في الهيدر | Shopify, Salla | نعم | `components/home/HomeHeaderActions.tsx` | spec جاهز `home-header-seller-cta-2026-06-16.md` |
| Wishlist hooks (platform-level) | Salla | نعم | `components/product/ProductCard.tsx` | specs جاهزة `*-wishlist-*.md` |

---

## إضافات لـ ideas-backlog

1. **`format=light` لـ List Products API** — `lib/api-server.ts` — تحسين أداء للمتاجر ذات الكتالوج الكبير؛ **تحذير:** يتعارض مع variant data المطلوبة لنقاط الألوان، يحتاج استراتيجية selective fetch.

---

## الخطوة التالية

- **spec هذه الجولة (W30):** نقاط ألوان Variants على `ProductCard` في صفحات المتجر — مُلهَم من Salla Twilight v2.14.420 + Shopify Horizon (تداخل نمط من منافسَين = أولوية مرتفعة).
- تفعيل exa OAuth لازال مطلوباً → تحقق حي من Salla/Zid/Shopify متاجر في W31.
- `salla-fulfillment-methods` لا يزال في ideas-backlog — يتوقف على تحقق من حقول API.
