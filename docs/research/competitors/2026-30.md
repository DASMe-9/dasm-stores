# تقرير استخبارات المنافسين — 2026-W30

**تاريخ التشغيل:** 2026-06-30
**الأساس الزمني:** `docs/research/competitors/2026-29.md` (2026-06-16)
**حالة exa:** غير متاحة — تتطلب OAuth في جلسة تفاعلية. استُخدم WebSearch بديلاً.

---

## ملاحظة منهجية

exa غير متاحة في هذه الجلسة (non-interactive). استُخدم WebSearch على:
- `docs.salla.dev` + `changelog.salla.sa`
- `zid.sa` + `help.zid.sa`
- `github.com/Shopify/dawn` + `themes.shopify.com`

---

## Delta جديد — W30

### 1. Salla Twilight v2.14.420 — تعديل الطلب مع إضافة منتجات جديدة

| التفصيل | القيمة |
|---------|--------|
| المصدر | `docs.salla.dev` — changelog Twilight |
| الوصف | العميل يستطيع الآن اختيار وإضافة منتجات جديدة أثناء تعديل طلب قائم (order editing). لم تكن المنتجات الجديدة قابلة للإضافة سابقاً — فقط تعديل الكميات |
| النمط البصري | صفحة تعديل الطلب تعرض شبكة منتجات مع زر "أضف منتجاً" — مشابه لتجربة Re-order |
| الانطباق على dasm-stores | **لا (مباشر)** — order editing خارج نطاق baseline المتسوق الحالي (`app/[slug]/track/`) |
| Delta جديد؟ | **نعم** — لم يُوثَّق سابقاً |

### 2. Salla Twilight v2.14.420 — إصلاحات Cart Loyalty Panel

| التفصيل | القيمة |
|---------|--------|
| المصدر | `docs.salla.dev` — changelog Twilight |
| الوصف | إصلاح عرض loyalty panel في صفحة السلة (مشكلة في حساب نقاط الولاء أثناء الدفع) |
| الانطباق على dasm-stores | **جزئياً** — `app/[slug]/cart/page.tsx` لا يعرض loyalty panel حالياً. التحقق من API: هل يُرجع الـ store endpoint بيانات loyalty? |
| Delta جديد؟ | **نعم** — لم يُوثَّق سابقاً |

### 3. Shopify Dawn — دعم Standard Storefront Events + Product Disclosures (17 يونيو 2026)

| التفصيل | القيمة |
|---------|--------|
| المصدر | `github.com/Shopify/dawn` releases + `themes.shopify.com` |
| الوصف | إضافة دعم لـ Standard Storefront Events (تتبع موحّد للإجراءات) + قسم/بلوك جديد لعرض "Product Disclosures" (إفصاحات المنتج — مكوّنات، تحذيرات، شروط) |
| النمط البصري لـ Product Disclosures | أكورديون قابل للتوسيع أسفل وصف المنتج يعرض: مكوّنات/مواد، تحذيرات السلامة، متطلبات تنظيمية |
| الانطباق على dasm-stores | **جزئياً** — `app/[slug]/products/[productId]/page.tsx` لا يعرض قسم إفصاح. البيانات تتوقف على ما يُعيده الـ API. مناسب للمنتجات الغذائية/التجميل/الإلكترونيات |
| الملف المتأثر | `app/[slug]/products/[productId]/page.tsx` |
| Delta جديد؟ | **نعم** — لم يُوثَّق سابقاً |

### 4. Zid — لا delta جديد مرصود

| التفصيل | القيمة |
|---------|--------|
| المصدر | `zid.sa` + `help.zid.sa` |
| الملاحظة | لم تُرصد تغييرات بصرية/مكوّنات جديدة من Zid لجولة W30. الموقع الرسمي ثابت على إطار الـ platform العام. |
| Delta جديد؟ | لا |

---

## أنماط مستمرة من 2026-29 (لا delta جديد)

| النمط | المنافس | الانطباق | الملف | الحالة |
|-------|---------|----------|-------|--------|
| header CTA "Start free trial" للضيوف | Shopify | نعم | `components/home/HomeHeaderActions.tsx` | **spec هذه الجولة** |
| مكوّن `salla-fulfillment-methods` (شحن/استلام في صفحة المنتج) | Salla | جزئياً | `app/[slug]/products/[productId]/page.tsx` | ideas-backlog — يتوقف على API |
| Quick Buy من صفحة الكتالوج | Salla | نعم | `components/product/ProductCard.tsx` | spec جاهز `product-card-quick-add-2026-06-13.md` — ينتظر Cursor |
| Sold-out overlay على بطاقة المنتج | Shopify, Salla | نعم | `components/product/ProductCard.tsx` | spec جاهز `product-card-sold-out-overlay-2026-06-14.md` — ينتظر Cursor |

---

## الخطوة التالية

- **spec هذه الجولة (W30):** زر "افتح متجرك مجاناً" في هيدر الصفحة الرئيسية للضيوف — مُلهَم من Shopify header CTA (تم توثيقه كـ delta جديد في W29، الآن ينتقل إلى spec).
- إضافة Shopify Product Disclosures إلى ideas-backlog — يتوقف على حقول API.
- تفعيل exa OAuth في جلسة تفاعلية لتحقق حي من متاجر Salla/Zid في W31.
