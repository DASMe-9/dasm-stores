# تقرير استخبارات المنافسين — 2026-W30

**تاريخ التشغيل:** 2026-06-17
**الأساس الزمني:** `docs/research/competitors/2026-29.md` (2026-06-16)
**حالة exa:** لم تُفعَّل — الجولة الثامنة على التوالي. الإجراء المطلوب: Connectors → exa → Authorize (OAuth).

---

## ملاحظة منهجية

exa غير متاحة هذه الجولة. استُخدمت:
- WebSearch على `changelog.salla.sa` و `docs.salla.dev`
- WebSearch على Shopify changelog و shopify.com
- WebSearch على `zid.sa` و `docs.zid.sa`

---

## Delta جديد — W30

### 1. Salla Twilight v2.14.420 — تنقل الكاروسيل عبر خيارات الألوان

| التفصيل | القيمة |
|---------|--------|
| المصدر | `docs.salla.dev` — changelog Twilight v2.14.420 |
| الوصف | إضافة تنقل Carousel من خيارات الألوان في مكوّني `salla-product-options` و `salla-slider` — عند تحديد المتسوق لون/خيار مرئي، تتغير صورة المنتج الرئيسية فوراً لعرض الخيار المحدد |
| النمط البصري | ضغط على خيار لون/نوع → الكاروسيل يقفز لصورة ذلك الخيار تلقائياً دون إعادة تحميل |
| الانطباق على dasm-stores | **جزئياً** — `components/product/ProductPurchaseSection.tsx` يتعامل مع خيارات المتغيرات (`selectedVariant`) لكن `ProductGallery.tsx` لا تُحدِّث صورتها تلقائياً بناءً على الخيار المحدد |
| الملف المتأثر | `components/product/ProductGallery.tsx` + `components/product/ProductPurchaseSection.tsx` |
| Delta جديد؟ | **نعم** — لم يُوثَّق سابقاً |

### 2. Salla — "الشراء السريع" (تخطي صفحة السلة)

| التفصيل | القيمة |
|---------|--------|
| المصدر | `changelog.salla.sa/1511903425` + `help.salla.sa` — آخر تحديث مارس 2026 |
| الوصف | ميزة "الشراء السريع" تنقل المتسوق من صفحة المنتج مباشرةً لصفحة الدفع دون المرور على سلة المشتريات — تقليل خطوات الشراء من 3 إلى 1 |
| النمط البصري | زر ثانٍ أسفل "أضف للسلة" بنص "اشترِ الآن" بلون مختلف (ثانوي) — ضغطه يُعيد التوجيه لـ checkout مباشرة |
| الانطباق على dasm-stores | **جزئياً** — `ProductPurchaseSection.tsx` يملك زر "أضف للسلة" فقط. تخطي السلة يتطلب إنشاء order مؤقت أو تمرير منتج مباشر لـ checkout |
| الملف المتأثر | `components/product/ProductPurchaseSection.tsx` + `app/[slug]/checkout/page.tsx` |
| Delta جديد؟ | **نعم** — النمط أُشير له في W29 كمكوّن `salla-fulfillment-methods` لكن هذا نمط مختلف: Quick Buy بدلاً من معلومات توصيل |

### 3. Shopify — تخصيص الثيم per-Market

| التفصيل | القيمة |
|---------|--------|
| المصدر | `changelog.shopify.com` — مارس 2026 |
| الوصف | تخصيص إعدادات الثيم وتضمينات التطبيقات لكل سوق/منطقة جغرافية بشكل منفصل — الألوان، الخطوط، CSS مخصص |
| الانطباق على dasm-stores | **لا** — منصة أحادية السوق (KSA). ميزة enterprise |
| Delta جديد؟ | لا — خارج نطاق الجولة |

### 4. Salla — تحديثات Wishlist Hooks مستمرة

| التفصيل | القيمة |
|---------|--------|
| الملاحظة | W29 أشار لـ wishlist hooks في Twilight v2.14.400. v2.14.420 لا يُضيف delta جديد على wishlist |
| الانطباق | لا تغيير — specs الـ wishlist الموجودة كافية |
| Delta جديد؟ | **لا** |

---

## أنماط مستمرة من 2026-29 (لا delta جديد)

| النمط | المنافس | الانطباق | الملف | الحالة |
|-------|---------|----------|-------|--------|
| زر "افتح متجرك" في الهيدر | Shopify, Salla, Zid | نعم | `components/home/HomeHeaderActions.tsx` | spec جاهز `home-header-seller-cta-2026-06-16.md` |
| Sticky Add-to-Cart على موبايل | Salla, Shopify | نعم | `components/store/StoreChrome.tsx` | spec جاهز `sticky-mini-cart-bar-2026-06-15.md` |
| Quick Add من الكتالوج | Salla | نعم | `components/product/ProductCard.tsx` | spec جاهز `product-card-quick-add-2026-06-13.md` |
| Sold-out overlay | Shopify, Salla | نعم | `components/product/ProductCard.tsx` | spec جاهز `product-card-sold-out-overlay-2026-06-14.md` |

---

## الخطوة التالية

- **spec هذه الجولة (W30):** Cart Add Toast — تأكيد بصري خفيف عند إضافة منتج للسلة بدلاً من فتح الـ drawer الكامل (مُلهَم من نمط Salla Quick Buy الذي يُعزز "تدفق أخف للمتسوق"). الأثر عالٍ / الجهد منخفض.
- تنقل الكاروسيل عبر خيارات الألوان (delta W30 جديد) → `ideas-backlog.md`.
- "الشراء السريع" (Quick Buy) → `ideas-backlog.md` — يتطلب تحقق من checkout flow.
- تفعيل exa OAuth لازال مطلوباً → تحقق حي من Salla/Zid/Shopify في W31.
