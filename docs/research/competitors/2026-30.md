# تقرير استخبارات المنافسين — 2026-W30

**تاريخ التشغيل:** 2026-06-19
**الأساس الزمني:** `docs/research/competitors/2026-29.md` (2026-06-16)
**حالة exa:** لم تُفعَّل — الجولة الثامنة على التوالي. الإجراء المطلوب: Connectors → exa → Authorize (OAuth).

---

## ملاحظة منهجية

exa غير متاحة هذه الجولة. استُخدمت:
- WebSearch على `changelog.salla.sa`، `docs.salla.dev`، `s.salla.sa/changelog`
- WebSearch على `github.com/Shopify/dawn/releases`
- WebSearch على `help.zid.sa`، `blog.zid.sa`، `docs.zid.sa`

---

## Delta جديد — W30

### 1. Salla Twilight 2.14.420 — الموالاة في السلة + تعديل الطلبات

| التفصيل | القيمة |
|---------|--------|
| المصدر | `s.salla.sa/changelog` |
| الوصف | إصلاح `salla-loyalty` JS Web Component في السلة (لوحة نقاط الولاء) + تمكين العميل من اختيار وإضافة منتجات جديدة أثناء تعديل الطلب |
| النمط البصري | لوحة نقاط في السلة: رصيد النقاط + شريط التقدم + "استبدل نقاطك" |
| الانطباق على dasm-stores | **جزئياً** — `CartDrawer.tsx` لا يعرض أي نظام نقاط ولاء. يتوقف على وجود API للولاء في المنصة |
| الملف المتأثر | `components/cart/CartDrawer.tsx` — إن كان API الولاء متاحاً |
| Delta جديد؟ | **نعم** — لم يُوثَّق سابقاً |

### 2. Salla — `salla-quick-buy` Component: تجاوز السلة للدفع المباشر

| التفصيل | القيمة |
|---------|--------|
| المصدر | `docs.salla.dev/doc-422725` — Salla Quick Buy docs (محدَّثة 2 يونيو 2026) |
| الوصف | `<salla-quick-buy>` يُنقل المتسوق مباشرة لصفحة الدفع متجاوزاً السلة بالكامل. "Instant purchase" يعمل عبر رابط أو زر مع pre-filled checkout |
| النمط | زر "اشترِ الآن" مستقل بجانب "أضف للسلة" على صفحة المنتج |
| الانطباق على dasm-stores | **نعم** — `app/[slug]/products/[productId]/page.tsx` يعرض "أضف للسلة" فقط؛ لا مسار checkout مباشر. يُعزز أولوية UX صفحة المنتج |
| الملف المتأثر | `app/[slug]/products/[productId]/page.tsx` |
| Delta جديد؟ | **نعم** — لم يُوثَّق سابقاً (Quick Buy السابق في W28 كان عن listing page، هذا عن checkout flow) |

### 3. Shopify Dawn 15.5 — Product Disclosures Section

| التفصيل | القيمة |
|---------|--------|
| المصدر | `github.com/Shopify/dawn/releases` — v15.5.0 (17 يونيو 2026) |
| الوصف | قسم وبلوك جديدان "product disclosures" على صفحة تفصيل المنتج: يعرض نصوصاً قانونية/سلامة/مكوّنات تُضاف تحت الوصف. دعم full storefront events |
| النمط | حقن نص مخصص منسَّق تحت وصف المنتج في صفحة التفصيل |
| الانطباق على dasm-stores | **جزئياً** — `app/[slug]/products/[productId]/page.tsx` لا يعرض أي نص تنبيهي/إفصاحي. مفيد للمتاجر ذات متطلبات امتثال (أغذية، مستحضرات، إلكترونيات) |
| الملف المتأثر | `app/[slug]/products/[productId]/page.tsx` |
| Delta جديد؟ | **نعم** — لم يُوثَّق سابقاً |

### 4. Zid — لا تحديثات واجهة مستجدة

| التفصيل | القيمة |
|---------|--------|
| المصدر | `help.zid.sa`، `blog.zid.sa`، `docs.zid.sa` |
| الوضع | هجرة Twig → Vitrin جارية منذ فبراير 2026؛ لا إطلاق ميزة جديدة في يونيو |
| Delta جديد؟ | **لا** |

---

## أنماط مستمرة من 2026-29 (لا delta جديد)

| النمط | المنافس | الانطباق | الملف | الحالة |
|-------|---------|----------|-------|--------|
| Wishlist hooks على مستوى platform | Salla | نعم | `ProductCard.tsx` + `app/page.tsx` | specs جاهزة، تنتظر Cursor |
| `salla-fulfillment-methods` على صفحة المنتج | Salla | جزئياً | `app/[slug]/products/[productId]/page.tsx` | مؤجل — تحقق API مطلوب |
| Seller header CTA | Shopify / Salla | نعم | `HomeHeaderActions.tsx` | spec جاهز `home-header-seller-cta-2026-06-16.md` |
| Sticky Add-to-Cart على الموبايل | Shopify / Salla | نعم | store mobile layout | spec جاهز `sticky-mini-cart-bar-2026-06-15.md` |

---

## الخطوة التالية

- **spec هذه الجولة (W30):** CartToast — إشعار خفيف بعد إضافة منتج للسلة (مُلهَم من نمط Salla cart UX + Twilight 2.14.420 loyalty interaction). الأثر: تأكيد فوري للمتسوق دون فتح DrawerCart بالكامل.
- إضافة `salla-quick-buy` (checkout مباشر) و product disclosures (Shopify) لـ ideas-backlog.
- تفعيل exa OAuth لازال مطلوباً → تحقق حي من متاجر Salla/Zid في W31.
