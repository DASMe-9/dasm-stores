# تقرير انحراف بصري — baseline-drift-2026-08-05

**تاريخ التشغيل:** 2026-08-05 (جولة أسبوعية — الأحد)
**مرجع الـ baseline:** `docs/design/baseline/marketplace-home.png` و `docs/design/baseline/subdomain-store.png`
**التقرير السابق:** `docs/research/baseline-drift-2026-06-16.md` (لا drift حرج)

---

## ملخص تنفيذي

**⚠️ DRIFT حرج — إعادة بناء كاملة لصفحة الـ marketplace.**

كوميتات `013f987` و `2a9372c` (2026-07-30) أعادت بناء `app/page.tsx` بالكامل تحت مسمى PR #271 "Elevate DASM Stores marketplace homepage". الـ hero تغيّر هيكلياً: شريط البحث انتقل خارج الـ hero، ظهر widget جديد (CommercePassport)، وأُضيف قسم "لأصحاب المتاجر" بين البحث والمنتجات.

**قرار المرحلة:** drift حرج مُكتشف → تتوقف الجولة عند المرحلة 1. لا مرحلة 2 ولا مرحلة 3 هذه الجولة.

**الكوميت المسبب الرئيسي:** `013f987` — "elevate DASM Stores marketplace homepage" (2026-07-30 19:28 AST)

---

## الانحرافات المكتشفة

### 1. شريط البحث — انتقل خارج الـ Hero (CRITICAL)

| البند | التفصيل |
|-------|---------|
| **المكوّن** | Hero (marketplace) |
| **الملف + السطر** | `app/page.tsx` سطر 501–529 |
| **الوصف البصري** | الـ baseline يُظهر شريط بحث مدمجاً داخل قسم الـ hero الرئيسي. حالياً الـ hero لا يحتوي أي شريط بحث؛ البحث انتقل إلى `<section>` مستقل أسفل الـ hero يحمل خلفية `bg-[#f4f0e8]`. |
| **متى تغيّر** | 2026-07-30 (كوميت `013f987`) |
| **توصية الاسترجاع** | نقل كتلة `<form>` (الشريط + `<Search>` + `<input id="marketplace-search">` + زر "بحث") لتكون داخل `<section data-testid="platform-hero">` بعد الـ CTA buttons — السطر التقريبي 487 في `app/page.tsx`. توصية فقط — تنتظر قرار محمد الزهراني. |

---

### 2. CommercePassport Widget — عنصر جديد غير موجود في الـ baseline (MAJOR)

| البند | التفصيل |
|-------|---------|
| **المكوّن** | Hero (marketplace) — العمود الأيمن |
| **الملف + السطر** | `app/page.tsx` سطر 275–332 (`CommercePassport`) و سطر 497 (الاستدعاء) |
| **الوصف البصري** | بطاقة "جواز نمو المتجر" تعرض: شريطاً علوياً بنص `DASM COMMERCE PASSPORT`، 5 مراحل مرقّمة (هوية المتجر / الكتالوج / إعداد الدفع / الشحن / الوصول للعميل)، وعداد للمتاجر المتاحة في الأسفل. الـ baseline يُظهر أيقونات مزايا المنصة في هذا الموضع (محل `baseline-drift-2026-06-07` — كان مقبولاً). العنصر الجديد كلياً لم يرد في أي baseline أو تقرير سابق. |
| **متى تغيّر** | 2026-07-30 (كوميت `013f987`) |
| **توصية الاسترجاع** | إما (أ) اعتماد CommercePassport عنصراً رسمياً وتحديث الـ baseline برسم صورة جديدة، أو (ب) استبدال CommercePassport بالأيقونات التي كانت مخططة في الـ baseline. القرار لمحمد الزهراني — لا تنفيذ. |

---

### 3. قسم "لأصحاب المتاجر" — مُضاف وغير موجود في الـ baseline (MAJOR)

| البند | التفصيل |
|-------|---------|
| **المكوّن** | Section بين شريط البحث وشبكة المنتجات |
| **الملف + السطر** | `app/page.tsx` سطر 532–579 |
| **الوصف البصري** | قسم `id="for-merchants"` يعرض: ترويسة "لأصحاب المتاجر" + عنوان "أدوات التشغيل في قصة واحدة مفهومة." + 4 بطاقات (رابط مستقل / كتالوج / تشغيل الطلبات / قراءة الأداء). لم يرد هذا القسم في الـ baseline ولا في أي تقرير drift سابق. |
| **متى تغيّر** | 2026-07-30 (كوميت `013f987`) |
| **توصية الاسترجاع** | إما (أ) اعتماده وتحديث الـ baseline، أو (ب) نقله لصفحة مستقلة `/merchant`. القرار لمحمد الزهراني — لا تنفيذ. |

---

## ملاحظة: Spec مُنفَّذ

`docs/specs/home-header-seller-cta-2026-06-16.md` (زر "افتح متجرك" في الهيدر) **طُبِّق** ضمن نفس كوميت الـ elevate (`013f987`). يمكن تحديث الـ spec بملاحظة "مُنفَّذ" في جولة قادمة.

---

## حالة الفجوات البصرية المستمرة من التقرير السابق

| المكوّن | العنصر | الحالة في الكود | القرار |
|---------|--------|-----------------|--------|
| Hero (marketplace) | أيقونات مزايا المنصة | **غائب** | مقبول بقرار التجميد 2026-06-07 |
| ProductTile (marketplace) | شارة «ممول» | **غائب** | مقبول بقرار التجميد |
| ProductTile (marketplace) | زر سلة دائري | `rounded-xl` | ينتظر Cursor (`product-tile-cart-button-2026-06-14.md`) |
| ProductTile (marketplace) | أيقونة قلب (مفضلة) | **غائب** | ينتظر Cursor (`product-tile-wishlist-2026-06-11.md`) |
| ProductCard (store pages) | أيقونة قلب (مفضلة) | **غائب** | ينتظر Cursor (`product-card-store-wishlist-2026-06-12.md`) |
| StoreInfoCard | وسوم ثقة | **غائب** | ينتظر Cursor (`store-info-trust-badges-2026-06-08.md`) |
| Marketplace footer | StatsBar | **غائب** | مقبول بقرار التجميد |
| Store (mobile) | Sticky Cart Bar | **غائب** | ينتظر Cursor (`sticky-mini-cart-bar-2026-06-15.md`) |

---

## الخطوة التالية

1. **محمد الزهراني** يراجع الانحرافات الثلاثة في هذا التقرير ويقرر:
   - (أ) تحديث baseline رسمياً بصورة marketplace-home.png الجديدة (PR منفصل `baseline-update`)، أو
   - (ب) استرجاع العناصر الثلاثة للحالة الـ baseline

2. **الجولة القادمة** تستأنف المراحل 2 و3 بعد حسم قرار الـ baseline.
